# Model Design Order Sheet

1. `umaxformer_v1`: rebuild the SODA plus ORAS5 validation baseline.
2. `umaxformer_v2`: rebuild the promoted ORAS5-only model.

Only these two configs are active. Historical ORAS5-only variants belong in `configs/archive/umaxformer_v1_legacy_oras5/`.
